{
  "topic": "03-control-flow",
  "exercises": [
    {
      "id": "ex-4-1",
      "title": "IF Statement",
      "difficulty": "beginner",
      "chapter": "5",
      "free": true,
      "description": "Use a basic IF statement to execute code conditionally.",
      "instructions": [
        "Create a variable ${age} with value 18",
        "Use IF to check if ${age} >= 18",
        "If true, log 'Adult'",
        "End with END"
      ],
      "initialCode": "*** Test Cases ***\nIF Test\n    ${age}    Set Variable    18\n    # Write IF statement here\n    ",
      "solution": "*** Test Cases ***\nIF Test\n    ${age}    Set Variable    18\n    IF    ${age} >= 18\n        Log    Adult\n    END",
      "hints": [
        "IF syntax: IF    <condition>",
        "Indentation inside IF is 8 spaces (double indent)",
        "Always close IF with END",
        "Comparison operators: ==, !=, <, >, <=, >="
      ],
      "validation": {
        "mustContain": [
          "IF",
          "END",
          "Adult"
        ],
        "mustPass": true
      },
      "theory": {
        "title": "Conditional Execution with IF Statements in Robot Framework",
        "content": "<div class=\"section-heading\">Introduction to Control Flow</div>\n\n<p class=\"story\">Control flow structures allow tests to make <strong>decisions</strong> and execute different code paths based on <strong>conditions</strong>. The IF statement is the fundamental building block for conditional logic in Robot Framework, enabling tests to adapt to dynamic situations and handle variable scenarios.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Why Conditional Logic Matters</div></p>\n\n<p class=\"story\"><strong>Without conditionals:</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nRigid Test\n    ${status}    Get User Status\n    Log    User status is active    # Always runs, even if status is inactive</code></div></p>\n\n<p class=\"story\"><strong>With conditionals:</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nAdaptive Test\n    ${status}    Get User Status\n    IF    '${status}' == 'active'\n        Log    User is active\n        Perform Active User Actions\n    ELSE\n        Log    User is inactive\n        Skip Test    User account is not active\n    END</code></div></p>\n\n<p class=\"story\">Conditionals make tests <strong>intelligent</strong>, <strong>flexible</strong>, and capable of handling <strong>real-world variability</strong>.</p>\n\n<div class=\"section-heading\">The IF Statement</div>\n\n<p class=\"story\">Robot Framework's IF statement (introduced in RF 4.0, enhanced in RF 5.0+) provides Python-like conditional execution within keyword-driven tests.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Basic Syntax</div></p>\n\n<div class=\"code-block\"><code>IF    <condition>\n    <keyword>\n    <keyword>\nEND</code></div>\n\n<p class=\"story\"><strong>Key elements:</strong>\n- <span class=\"inline-code\">IF</span> keyword starts the conditional block\n- Condition is evaluated as Python expression\n- Body keywords are indented <strong>8 spaces</strong> (double the standard 4)\n- <span class=\"inline-code\">END</span> closes the block</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Example: Age Check</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nAge Verification\n    ${age}    Set Variable    18\n    IF    ${age} >= 18\n        Log    Adult\n    END</code></div>\n\n<p class=\"story\"><strong>Execution flow:</strong>\n1. Variable <span class=\"inline-code\">${age}</span> is set to 18\n2. Condition <span class=\"inline-code\">${age} >= 18</span> evaluates to <span class=\"inline-code\">True</span>\n3. <span class=\"inline-code\">Log Adult</span> executes\n4. IF block ends</p>\n\n<p class=\"story\">If <span class=\"inline-code\">${age}</span> were 16, the Log statement would be <strong>skipped</strong>.</p>\n\n<div class=\"section-heading\">Condition Evaluation</div>\n\n<p class=\"story\">Conditions are evaluated as <strong>Python expressions</strong> using Python's truthiness rules.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Comparison Operators</div></p>\n\n<div class=\"code-block\"><code># Numeric comparisons\nIF    ${age} >= 18        # Greater than or equal\nIF    ${count} < 10       # Less than\nIF    ${score} > 50       # Greater than\nIF    ${value} <= 100     # Less than or equal\nIF    ${x} == ${y}        # Equal\nIF    ${x} != ${y}        # Not equal</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">String Comparisons</div></p>\n\n<div class=\"code-block\"><code># String equality (case-sensitive)\nIF    '${status}' == 'active'\nIF    '${name}' != 'admin'\n\n<p class=\"story\"># String methods\nIF    '${email}'.endswith('@example.com')\nIF    'error' in '${message}'\nIF    '${text}'.startswith('Hello')</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Boolean Conditions</div></p>\n\n<div class=\"code-block\"><code># Direct boolean values\n${is_valid}    Set Variable    ${True}\nIF    ${is_valid}\n    Log    Validation passed\nEND\n\n<p class=\"story\"># Boolean operators\nIF    ${age} >= 18 and ${has_license}\n    Log    Can drive\nEND</p>\n\n<p class=\"story\">IF    ${is_admin} or ${is_moderator}\n    Log    Has elevated privileges\nEND</p>\n\n<p class=\"story\">IF    not ${is_blocked}\n    Log    User is active\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Complex Conditions</div></p>\n\n<div class=\"code-block\"><code># Multiple conditions\nIF    ${age} >= 18 and '${country}' == 'US' and ${has_id}\n    Log    Eligible for service\nEND\n\n<p class=\"story\"># Parentheses for clarity\nIF    (${score} > 90 and ${attendance} > 80) or ${extra_credit}\n    Log    Pass with distinction\nEND</code></div></p>\n\n<div class=\"section-heading\">Indentation Rules for IF Blocks</div>\n\n<p class=\"story\">Robot Framework uses <strong>double indentation</strong> (8 spaces) for IF block contents:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nIndentation Example\n    ${age}    Set Variable    18    # 4 spaces (test case level)\n    IF    ${age} >= 18              # 4 spaces (test case level)\n        Log    Adult                # 8 spaces (inside IF block)\n        Log    Can vote             # 8 spaces (inside IF block)\n    END                              # 4 spaces (test case level)</code></div>\n\n<p class=\"story\"><strong>Visual guide:</strong>\n<div class=\"code-block\"><code>····${age}····Set·Variable····18\n····IF····${age}·>=·18\n········Log····Adult\n········Log····Can·vote\n····END</code></div></p>\n\n<p class=\"story\">Where <span class=\"inline-code\">·</span> represents a space character.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Common Indentation Errors</div></p>\n\n<div class=\"code-block\"><code># Wrong: IF body uses 4 spaces (same as test level)\nIF    ${age} >= 18\n    Log    Adult    # ERROR: Not recognized as inside IF\nEND\n\n<p class=\"story\"># Wrong: IF body uses 12 spaces (triple indent)\nIF    ${age} >= 18\n            Log    Adult    # ERROR: Over-indented\nEND</p>\n\n<p class=\"story\"># Correct: IF body uses 8 spaces\nIF    ${age} >= 18\n        Log    Adult    # Properly indented\nEND</code></div></p>\n\n<div class=\"section-heading\">Practical Use Cases</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Conditional Test Execution</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nEnvironment-Specific Test\n    ${env}    Get Environment Variable    ENV\n    IF    '${env}' == 'production'\n        Log    Running in production mode\n        Use Production API Endpoint\n    END\n    IF    '${env}' == 'staging'\n        Log    Running in staging mode\n        Use Staging API Endpoint\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Feature Flag Handling</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nFeature Toggle Test\n    ${feature_enabled}    Check Feature Flag    new_checkout\n    IF    ${feature_enabled}\n        Test New Checkout Flow\n    END\n    IF    not ${feature_enabled}\n        Log    Feature disabled, using old checkout\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Error Handling</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nConditional Validation\n    ${response}    API Call    /user/123\n    ${status}      Get From Dictionary    ${response}    status\n    \n    IF    ${status} == 200\n        Log    Request successful\n        Validate Response Data    ${response}\n    END\n    IF    ${status} >= 400\n        Log    Request failed with status ${status}\n        Fail    API returned error\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Data Validation</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nInput Validation\n    ${email}    Get User Input    email\n    \n    IF    '@' not in '${email}'\n        Fail    Invalid email format: missing @\n    END\n    IF    '${email}'.count('@') > 1\n        Fail    Invalid email format: multiple @\n    END\n    \n    Log    Email format is valid</code></div>\n\n<div class=\"section-heading\">Comparison with Older RF Versions</div>\n\n<p class=\"story\">Robot Framework 4.0+ introduced inline IF. Prior versions used <span class=\"inline-code\">Run Keyword If</span>:</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Old Style (RF 3.x)</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nOld Conditional\n    ${age}    Set Variable    18\n    Run Keyword If    ${age} >= 18    Log    Adult</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">New Style (RF 4.0+)</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nNew Conditional\n    ${age}    Set Variable    18\n    IF    ${age} >= 18\n        Log    Adult\n    END</code></div>\n\n<p class=\"story\"><strong>Benefits of new syntax:</strong>\n- More readable and Pythonic\n- Supports multi-line blocks naturally\n- Easier to nest conditions\n- Consistent with modern programming languages</p>\n\n<div class=\"section-heading\">Best Practices</div>\n\n<p class=\"story\">#<div class=\"section-heading\">1. Use Descriptive Variable Names</div></p>\n\n<div class=\"code-block\"><code># Poor\nIF    ${x} > ${y}\n    Log    Condition met\nEND\n\n<p class=\"story\"># Good\nIF    ${user_age} >= ${minimum_age}\n    Log    User meets age requirement\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">2. Keep Conditions Simple</div></p>\n\n<div class=\"code-block\"><code># Complex (hard to read)\nIF    ${a} > 10 and ${b} < 5 and ('${c}' == 'yes' or '${d}' == 'active')\n    Log    Complex condition\nEND\n\n<p class=\"story\"># Better: Extract to variable\n${condition1}    Evaluate    ${a} > 10 and ${b} < 5\n${condition2}    Evaluate    '${c}' == 'yes' or '${d}' == 'active'\nIF    ${condition1} and ${condition2}\n    Log    Condition met\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">3. Always Close with END</div></p>\n\n<div class=\"code-block\"><code># Wrong: Missing END\nIF    ${age} >= 18\n    Log    Adult\n# Next keyword will be part of IF block by mistake!\n\n<p class=\"story\"># Correct\nIF    ${age} >= 18\n    Log    Adult\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">4. Quote String Literals</div></p>\n\n<div class=\"code-block\"><code># Can cause issues if variable contains special characters\nIF    ${status} == active\n    Log    Active\nEND\n\n<p class=\"story\"># Safe: Quote string literals\nIF    '${status}' == 'active'\n    Log    Active\nEND</code></div></p>\n\n<p class=\"story\">By mastering IF statements, you unlock <strong>conditional logic</strong> in Robot Framework, enabling tests to handle dynamic scenarios, validate data conditionally, and adapt to different environments and configurations.</p>",
        "learningObjectives": [
          "Understand the purpose and syntax of IF statements in Robot Framework",
          "Write conditional logic using comparison operators",
          "Apply proper 8-space indentation for IF block contents",
          "Use IF statements to create adaptive, intelligent test cases"
        ],
        "relatedConcepts": [
          "ELSE and ELSE IF branches",
          "Python expression evaluation",
          "Boolean logic and operators",
          "Run Keyword If (legacy syntax)",
          "Nested conditionals"
        ],
        "documentation": "https://robotframework.org/robotframework/latest/RobotFrameworkUserGuide.html#if-else-syntax",
        "estimatedReadTime": "6-7 minutes"
      },
      "metadata": {
        "tags": [
          "beginner",
          "control-flow",
          "if-statement",
          "conditional-logic",
          "comparison-operators",
          "indentation",
          "decision-making",
          "python-expressions"
        ],
        "estimatedTime": "10-15 minutes",
        "prerequisites": [
          "ex-1-4",
          "ex-1-3"
        ],
        "nextExercises": [
          "ex-4-2"
        ],
        "version": "2.0",
        "lastUpdated": "2025-10-02"
      }
    },
    {
      "id": "ex-4-2",
      "title": "IF-ELSE Statement",
      "difficulty": "beginner",
      "chapter": "5",
      "free": true,
      "description": "Implement two-branch conditional logic.",
      "instructions": [
        "Create ${score} with value 45",
        "IF ${score} >= 50, log 'Pass'",
        "ELSE log 'Fail'",
        "Close with END"
      ],
      "initialCode": "*** Test Cases ***\nIF ELSE Test\n    ${score}    Set Variable    45\n    # Write IF-ELSE here\n    ",
      "solution": "*** Test Cases ***\nIF ELSE Test\n    ${score}    Set Variable    45\n    IF    ${score} >= 50\n        Log    Pass\n    ELSE\n        Log    Fail\n    END",
      "hints": [
        "ELSE comes after the IF block",
        "ELSE has same indentation as IF (4 spaces)",
        "Code inside ELSE is indented 8 spaces",
        "Don't forget END at the end"
      ],
      "validation": {
        "mustContain": [
          "IF",
          "ELSE",
          "END",
          "Fail"
        ],
        "mustPass": true
      },
      "theory": {
        "title": "Two-Branch Conditional Logic with IF-ELSE",
        "content": "<div class=\"section-heading\">Expanding Conditionals: IF-ELSE</div>\n\n<p class=\"story\">While basic IF statements execute code when a condition is true, real-world scenarios often require <strong>alternative actions</strong> when conditions are false. The IF-ELSE structure provides <strong>two-branch decision making</strong>, ensuring one of two code paths always executes.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">The Need for ELSE</div></p>\n\n<p class=\"story\"><strong>IF only (incomplete logic):</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nIncomplete Test\n    ${score}    Set Variable    45\n    IF    ${score} >= 50\n        Log    Pass\n    END\n    # What happens if score < 50? Nothing!</code></div></p>\n\n<p class=\"story\"><strong>IF-ELSE (complete logic):</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nComplete Test\n    ${score}    Set Variable    45\n    IF    ${score} >= 50\n        Log    Pass\n    ELSE\n        Log    Fail\n    END\n    # Either Pass or Fail always executes</code></div></p>\n\n<p class=\"story\">The ELSE branch ensures <strong>explicit handling</strong> of the false condition, making test behavior predictable and complete.</p>\n\n<div class=\"section-heading\">IF-ELSE Syntax</div>\n\n<div class=\"code-block\"><code>IF    <condition>\n    <keywords executed if true>\nELSE\n    <keywords executed if false>\nEND</code></div>\n\n<p class=\"story\"><strong>Structure:</strong>\n- <span class=\"inline-code\">IF</span> at 4-space indentation (test level)\n- IF body at 8-space indentation\n- <span class=\"inline-code\">ELSE</span> at 4-space indentation (same as IF)\n- ELSE body at 8-space indentation\n- <span class=\"inline-code\">END</span> at 4-space indentation (closes entire block)</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Visual Breakdown</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nScore Evaluation\n····${score}····Set·Variable····45    # 4 spaces: test level\n····IF····${score}·>=·50              # 4 spaces: IF starts\n········Log····Pass                    # 8 spaces: inside IF\n····ELSE                               # 4 spaces: ELSE at same level as IF\n········Log····Fail                    # 8 spaces: inside ELSE\n····END                                # 4 spaces: closes IF-ELSE block</code></div>\n\n<div class=\"section-heading\">Execution Flow</div>\n\n<p class=\"story\"><strong>When condition is TRUE:</strong>\n<div class=\"code-block\"><code>${score}    Set Variable    75\nIF    ${score} >= 50\n    Log    Pass         # ✓ Executes\nELSE\n    Log    Fail         # ✗ Skipped\nEND\n# Result: Logs \"Pass\"</code></div></p>\n\n<p class=\"story\"><strong>When condition is FALSE:</strong>\n<div class=\"code-block\"><code>${score}    Set Variable    45\nIF    ${score} >= 50\n    Log    Pass         # ✗ Skipped\nELSE\n    Log    Fail         # ✓ Executes\nEND\n# Result: Logs \"Fail\"</code></div></p>\n\n<p class=\"story\"><strong>Key principle:</strong> Exactly <strong>one branch</strong> always executes. Never both, never neither.</p>\n\n<div class=\"section-heading\">Practical Applications</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Pass/Fail Evaluation</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nExam Result\n    ${score}    Get Student Score    student_id=123\n    \n    IF    ${score} >= 60\n        Log    Student passed\n        Update Status    student_id=123    status=PASS\n        Send Congratulations Email\n    ELSE\n        Log    Student failed\n        Update Status    student_id=123    status=FAIL\n        Schedule Retake\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Environment-Based Configuration</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nEnvironment Setup\n    ${env}    Get Environment Variable    ENVIRONMENT\n    \n    IF    '${env}' == 'production'\n        Log    Using production settings\n        ${api_url}    Set Variable    https://api.prod.com\n        ${timeout}    Set Variable    ${30}\n    ELSE\n        Log    Using test/dev settings\n        ${api_url}    Set Variable    https://api.test.com\n        ${timeout}    Set Variable    ${10}\n    END\n    \n    Connect To API    ${api_url}    timeout=${timeout}</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Feature Availability Checks</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nConditional Test Execution\n    ${feature_available}    Check Feature    premium_dashboard\n    \n    IF    ${feature_available}\n        Log    Testing premium features\n        Navigate To Premium Dashboard\n        Verify Premium Features Visible\n    ELSE\n        Log    Premium features not available\n        Navigate To Basic Dashboard\n        Verify Basic Features Only\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">User Type Handling</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nUser-Specific Actions\n    ${user_type}    Get Current User Type\n    \n    IF    '${user_type}' == 'admin'\n        Log    Admin user detected\n        Verify Admin Menu Visible\n        Access Admin Panel\n    ELSE\n        Log    Regular user detected\n        Verify Admin Menu Not Visible\n        Access User Dashboard\n    END</code></div>\n\n<div class=\"section-heading\">Multiple Statements in Each Branch</div>\n\n<p class=\"story\">Both IF and ELSE branches can contain <strong>multiple keywords</strong>:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nComplex Branching\n    ${age}    Set Variable    16\n    \n    IF    ${age} >= 18\n        Log    User is an adult\n        Grant Full Access\n        Enable All Features\n        Log To File    adult_access.log    User granted full access\n    ELSE\n        Log    User is a minor\n        Grant Limited Access\n        Enable Safe Features Only\n        Log To File    minor_access.log    User granted limited access\n        Notify Parent\n    END</code></div>\n\n<p class=\"story\">Each branch executes its <strong>entire block</strong> of keywords when triggered.</p>\n\n<div class=\"section-heading\">Common Patterns</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Boolean Toggle</div></p>\n\n<div class=\"code-block\"><code>${is_enabled}    Get Feature Status    dark_mode\n\n<p class=\"story\">IF    ${is_enabled}\n    Log    Dark mode is ON\n    Apply Dark Theme\nELSE\n    Log    Dark mode is OFF\n    Apply Light Theme\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Null/Empty Checks</div></p>\n\n<div class=\"code-block\"><code>${username}    Get Username\n\n<p class=\"story\">IF    '${username}' != ''\n    Log    Username: ${username}\n    Proceed With Login\nELSE\n    Log    No username provided\n    Fail    Username is required\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Status Code Handling</div></p>\n\n<div class=\"code-block\"><code>${response}    Make API Request    /users/123\n${status}      Get Status Code     ${response}\n\n<p class=\"story\">IF    ${status} == 200\n    Log    Request successful\n    ${data}    Parse Response Body    ${response}\n    Validate User Data    ${data}\nELSE\n    Log    Request failed with status ${status}\n    Log    Response: ${response}\n    Fail    API request failed\nEND</code></div></p>\n\n<div class=\"section-heading\">Nested IF-ELSE</div>\n\n<p class=\"story\">IF-ELSE blocks can be <strong>nested</strong> for complex decision trees:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nNested Conditions\n    ${age}     Set Variable    25\n    ${country} Set Variable    US\n    \n    IF    ${age} >= 18\n        Log    User is adult\n        IF    '${country}' == 'US'\n            Log    US adult user\n            Apply US Adult Rules\n        ELSE\n            Log    International adult user\n            Apply International Adult Rules\n        END\n    ELSE\n        Log    User is minor\n        Apply Minor Restrictions\n    END</code></div>\n\n<p class=\"story\"><strong>Indentation for nested IF:</strong>\n- Outer IF body: 8 spaces\n- Inner IF keyword: 8 spaces (within outer IF)\n- Inner IF body: 12 spaces (within inner IF)\n- Inner ELSE: 8 spaces (within outer IF)\n- Inner ELSE body: 12 spaces</p>\n\n<div class=\"section-heading\">Common Mistakes</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Missing END</div></p>\n\n<div class=\"code-block\"><code># Wrong: No END keyword\nIF    ${score} >= 50\n    Log    Pass\nELSE\n    Log    Fail\n# Next keyword will cause error!\n\n<p class=\"story\"># Correct\nIF    ${score} >= 50\n    Log    Pass\nELSE\n    Log    Fail\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Wrong Indentation</div></p>\n\n<div class=\"code-block\"><code># Wrong: ELSE has 8-space indent (should be 4)\nIF    ${score} >= 50\n    Log    Pass\n        ELSE\n    Log    Fail\nEND\n\n<p class=\"story\"># Correct: ELSE at same level as IF\nIF    ${score} >= 50\n    Log    Pass\nELSE\n    Log    Fail\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Multiple ELSE Blocks</div></p>\n\n<div class=\"code-block\"><code># Wrong: Can't have multiple ELSE\nIF    ${score} >= 50\n    Log    Pass\nELSE\n    Log    Fail\nELSE\n    Log    Another fail\nEND\n\n<p class=\"story\"># Correct: Use ELSE IF for multiple conditions (next lesson)</code></div></p>\n\n<div class=\"section-heading\">Comparison with Run Keyword If (Legacy)</div>\n\n<p class=\"story\"><strong>Old syntax (RF 3.x):</strong>\n<div class=\"code-block\"><code>${score}    Set Variable    45\nRun Keyword If    ${score} >= 50    Log    Pass\n...    ELSE                         Log    Fail</code></div></p>\n\n<p class=\"story\"><strong>New syntax (RF 4.0+):</strong>\n<div class=\"code-block\"><code>${score}    Set Variable    45\nIF    ${score} >= 50\n    Log    Pass\nELSE\n    Log    Fail\nEND</code></div></p>\n\n<p class=\"story\">The new syntax is <strong>clearer</strong>, <strong>more maintainable</strong>, and <strong>supports complex multi-line branches</strong> naturally.</p>\n\n<div class=\"section-heading\">Best Practices</div>\n\n<p class=\"story\">#<div class=\"section-heading\">1. Make Conditions Explicit</div></p>\n\n<div class=\"code-block\"><code># Implicit (relies on Python truthiness)\nIF    ${result}\n    Log    Success\nELSE\n    Log    Failure\nEND\n\n<p class=\"story\"># Explicit (clearer intent)\nIF    ${result} == ${True}\n    Log    Success\nELSE\n    Log    Failure\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">2. Log Branch Taken</div></p>\n\n<div class=\"code-block\"><code>IF    ${score} >= 50\n    Log    Condition TRUE: Score passed threshold\n    Mark As Passed\nELSE\n    Log    Condition FALSE: Score below threshold\n    Mark As Failed\nEND</code></div>\n\n<p class=\"story\">Logging which branch executes aids <strong>debugging</strong> when reviewing test logs.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">3. Keep Branches Balanced</div></p>\n\n<p class=\"story\">Avoid significantly different complexity between branches:</p>\n\n<div class=\"code-block\"><code># Unbalanced (IF has 1 line, ELSE has 20)\nIF    ${feature_enabled}\n    Use New Feature\nELSE\n    # 20 lines of complex fallback logic\n    ...\nEND\n\n<p class=\"story\"># Better: Extract to keywords\nIF    ${feature_enabled}\n    Use New Feature\nELSE\n    Use Legacy Feature With Workarounds\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">4. Use Descriptive Variable Names in Conditions</div></p>\n\n<div class=\"code-block\"><code># Poor\nIF    ${x} > ${y}\n    Do Something\nELSE\n    Do Something Else\nEND\n\n<p class=\"story\"># Good\nIF    ${actual_score} >= ${passing_threshold}\n    Mark Test As Passed\nELSE\n    Mark Test As Failed\nEND</code></div></p>\n\n<p class=\"story\">By mastering IF-ELSE, you create <strong>complete conditional logic</strong> that handles both success and failure paths, making tests robust and predictable in all scenarios.</p>",
        "learningObjectives": [
          "Implement two-branch conditional logic with IF-ELSE statements",
          "Understand execution flow where exactly one branch always runs",
          "Apply proper indentation for ELSE blocks (same level as IF)",
          "Use IF-ELSE for complete conditional handling in test scenarios"
        ],
        "relatedConcepts": [
          "ELSE IF for multi-branch conditions",
          "Nested IF-ELSE statements",
          "Boolean logic and truthiness",
          "Error handling with conditionals",
          "Legacy Run Keyword If ELSE syntax"
        ],
        "documentation": "https://robotframework.org/robotframework/latest/RobotFrameworkUserGuide.html#if-else-syntax",
        "estimatedReadTime": "6-7 minutes"
      },
      "metadata": {
        "tags": [
          "beginner",
          "control-flow",
          "if-else",
          "conditional-logic",
          "branching",
          "decision-making",
          "execution-flow",
          "two-branch"
        ],
        "estimatedTime": "10-15 minutes",
        "prerequisites": [
          "ex-4-1"
        ],
        "nextExercises": [
          "ex-4-3"
        ],
        "version": "2.0",
        "lastUpdated": "2025-10-02"
      }
    },
    {
      "id": "ex-4-3",
      "title": "IF-ELSE IF-ELSE",
      "difficulty": "beginner",
      "chapter": "5",
      "free": true,
      "description": "Handle multiple conditions with ELSE IF.",
      "instructions": [
        "Create ${grade} with value 75",
        "IF ${grade} >= 90, log 'A'",
        "ELSE IF ${grade} >= 70, log 'B'",
        "ELSE log 'C'",
        "Close with END"
      ],
      "initialCode": "*** Test Cases ***\nMultiple Conditions\n    ${grade}    Set Variable    75\n    # Write IF-ELSE IF-ELSE here\n    ",
      "solution": "*** Test Cases ***\nMultiple Conditions\n    ${grade}    Set Variable    75\n    IF    ${grade} >= 90\n        Log    A\n    ELSE IF    ${grade} >= 70\n        Log    B\n    ELSE\n        Log    C\n    END",
      "hints": [
        "ELSE IF allows checking another condition",
        "ELSE IF has same indentation as IF",
        "You can have multiple ELSE IF branches",
        "ELSE is optional but comes last"
      ],
      "validation": {
        "mustContain": [
          "IF",
          "ELSE IF",
          "ELSE",
          "END",
          "B"
        ],
        "mustPass": true
      },
      "theory": {
        "title": "Multi-Branch Conditionals with ELSE IF",
        "content": "<div class=\"section-heading\">Beyond Two Choices: ELSE IF</div>\n\n<p class=\"story\">Real-world logic often involves <strong>multiple conditions</strong> to evaluate, not just true/false scenarios. The ELSE IF clause enables <strong>multi-branch decision making</strong>, where different actions execute based on which condition is met first.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Why ELSE IF Matters</div></p>\n\n<p class=\"story\"><strong>Limited to two options (IF-ELSE):</strong>\n<div class=\"code-block\"><code>${grade}    Set Variable    75\nIF    ${grade} >= 90\n    Log    A\nELSE\n    Log    Not A    # Too vague - could be B, C, D, or F!\nEND</code></div></p>\n\n<p class=\"story\"><strong>Multiple options (IF-ELSE IF-ELSE):</strong>\n<div class=\"code-block\"><code>${grade}    Set Variable    75\nIF    ${grade} >= 90\n    Log    A\nELSE IF    ${grade} >= 80\n    Log    B\nELSE IF    ${grade} >= 70\n    Log    C\nELSE IF    ${grade} >= 60\n    Log    D\nELSE\n    Log    F\nEND\n# Result: Logs \"C\" (75 >= 70)</code></div></p>\n\n<p class=\"story\">ELSE IF provides <strong>precise branching</strong> for complex decision trees.</p>\n\n<div class=\"section-heading\">ELSE IF Syntax</div>\n\n<div class=\"code-block\"><code>IF    <condition1>\n    <keywords>\nELSE IF    <condition2>\n    <keywords>\nELSE IF    <condition3>\n    <keywords>\nELSE\n    <keywords>\nEND</code></div>\n\n<p class=\"story\"><strong>Structure:</strong>\n- <span class=\"inline-code\">IF</span> at 4-space indentation\n- Each <span class=\"inline-code\">ELSE IF</span> at 4-space indentation (same as IF)\n- <span class=\"inline-code\">ELSE</span> at 4-space indentation (optional, comes last)\n- All bodies at 8-space indentation\n- Single <span class=\"inline-code\">END</span> closes entire block</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Visual Indentation</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nGrade Calculation\n····${grade}····Set·Variable····75\n····IF····${grade}·>=·90         # 4 spaces\n········Log····A                  # 8 spaces\n····ELSE·IF····${grade}·>=·70    # 4 spaces\n········Log····B                  # 8 spaces\n····ELSE                          # 4 spaces\n········Log····C                  # 8 spaces\n····END                           # 4 spaces</code></div>\n\n<div class=\"section-heading\">Execution Flow: First Match Wins</div>\n\n<p class=\"story\"><strong>Critical behavior:</strong> Conditions are evaluated <strong>top to bottom</strong>, and the <strong>first true condition</strong> executes its block. Remaining conditions are <strong>skipped</strong>.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Example: Grade 75</div></p>\n\n<div class=\"code-block\"><code>${grade}    Set Variable    75\n\n<p class=\"story\">IF    ${grade} >= 90        # FALSE (75 >= 90) - Skip\nELSE IF    ${grade} >= 70   # TRUE (75 >= 70)  - EXECUTE & EXIT\nELSE IF    ${grade} >= 60   # Not evaluated (already matched above)\nELSE                         # Not evaluated\nEND</p>\n\n<p class=\"story\"># Result: Logs \"B\"</code></div></p>\n\n<p class=\"story\"><strong>Key insight:</strong> Even though <span class=\"inline-code\">${grade} >= 60</span> is also true, it's <strong>never checked</strong> because a previous condition matched.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Order Matters!</div></p>\n\n<p class=\"story\"><strong>Wrong order (broken logic):</strong>\n<div class=\"code-block\"><code>${grade}    Set Variable    95</p>\n\n<p class=\"story\">IF    ${grade} >= 60        # TRUE - Executes \"D\" and exits!\n    Log    D\nELSE IF    ${grade} >= 90   # Never reached\n    Log    A\nEND\n# Result: Logs \"D\" instead of \"A\" - WRONG!</code></div></p>\n\n<p class=\"story\"><strong>Correct order (most specific first):</strong>\n<div class=\"code-block\"><code>${grade}    Set Variable    95</p>\n\n<p class=\"story\">IF    ${grade} >= 90        # TRUE - Executes \"A\" and exits\n    Log    A\nELSE IF    ${grade} >= 60   # Never reached (already matched)\n    Log    D\nEND\n# Result: Logs \"A\" - CORRECT!</code></div></p>\n\n<p class=\"story\"><strong>Rule:</strong> Order conditions from <strong>most specific</strong> to <strong>least specific</strong> (highest values first in >= comparisons).</p>\n\n<div class=\"section-heading\">Practical Applications</div>\n\n<p class=\"story\">#<div class=\"section-heading\">HTTP Status Code Handling</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nAPI Response Handling\n    ${response}    Make HTTP Request    /api/users\n    ${status}      Get Status Code       ${response}\n    \n    IF    ${status} == 200\n        Log    Success - OK\n        Process Response Data    ${response}\n    ELSE IF    ${status} == 201\n        Log    Success - Created\n        Process New Resource      ${response}\n    ELSE IF    ${status} == 204\n        Log    Success - No Content\n        # No body to process\n    ELSE IF    ${status} >= 400 and ${status} < 500\n        Log    Client Error: ${status}\n        Handle Client Error    ${response}\n    ELSE IF    ${status} >= 500\n        Log    Server Error: ${status}\n        Handle Server Error    ${response}\n    ELSE\n        Log    Unexpected status: ${status}\n        Fail    Unknown HTTP status code\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Priority-Based Processing</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTicket Assignment\n    ${priority}    Get Ticket Priority    ticket_id=456\n    \n    IF    '${priority}' == 'critical'\n        Log    Critical priority - immediate action\n        Assign To Senior Engineer\n        Set SLA    4 hours\n        Send Alert To Manager\n    ELSE IF    '${priority}' == 'high'\n        Log    High priority - urgent\n        Assign To Available Engineer\n        Set SLA    24 hours\n    ELSE IF    '${priority}' == 'medium'\n        Log    Medium priority - normal queue\n        Add To Queue\n        Set SLA    3 days\n    ELSE\n        Log    Low priority - backlog\n        Add To Backlog\n        Set SLA    7 days\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">User Permission Levels</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nAccess Control\n    ${role}    Get User Role    user_id=123\n    \n    IF    '${role}' == 'admin'\n        Log    Admin access granted\n        Navigate To Admin Panel\n        Verify All Features Visible\n    ELSE IF    '${role}' == 'moderator'\n        Log    Moderator access granted\n        Navigate To Moderator Dashboard\n        Verify Moderation Tools Visible\n    ELSE IF    '${role}' == 'user'\n        Log    Standard user access\n        Navigate To User Dashboard\n        Verify Basic Features Only\n    ELSE\n        Log    Guest access (restricted)\n        Navigate To Public Pages Only\n        Verify Login Required For Features\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Age-Based Content Filtering</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nContent Rating Filter\n    ${age}    Get User Age    user_id=789\n    \n    IF    ${age} >= 18\n        Log    Adult content allowed\n        ${filter}    Set Variable    none\n    ELSE IF    ${age} >= 13\n        Log    Teen content filter\n        ${filter}    Set Variable    moderate\n    ELSE\n        Log    Child content filter\n        ${filter}    Set Variable    strict\n    END\n    \n    Apply Content Filter    ${filter}</code></div>\n\n<div class=\"section-heading\">Multiple ELSE IF Clauses</div>\n\n<p class=\"story\">You can have <strong>unlimited ELSE IF branches</strong>:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTemperature Classification\n    ${temp}    Get Current Temperature\n    \n    IF    ${temp} >= 100\n        Log    Extremely hot\n    ELSE IF    ${temp} >= 85\n        Log    Very hot\n    ELSE IF    ${temp} >= 70\n        Log    Hot\n    ELSE IF    ${temp} >= 60\n        Log    Warm\n    ELSE IF    ${temp} >= 50\n        Log    Mild\n    ELSE IF    ${temp} >= 40\n        Log    Cool\n    ELSE IF    ${temp} >= 32\n        Log    Cold\n    ELSE\n        Log    Freezing\n    END</code></div>\n\n<p class=\"story\">Each ELSE IF is evaluated <strong>only if all previous conditions were false</strong>.</p>\n\n<div class=\"section-heading\">ELSE Clause: Optional Safety Net</div>\n\n<p class=\"story\">The final ELSE is <strong>optional</strong> but recommended:</p>\n\n<p class=\"story\"><strong>Without ELSE (risky):</strong>\n<div class=\"code-block\"><code>IF    '${status}' == 'active'\n    Proceed With Test\nELSE IF    '${status}' == 'pending'\n    Wait For Activation\nEND\n# What if status is 'inactive' or 'unknown'? Silent failure!</code></div></p>\n\n<p class=\"story\"><strong>With ELSE (safer):</strong>\n<div class=\"code-block\"><code>IF    '${status}' == 'active'\n    Proceed With Test\nELSE IF    '${status}' == 'pending'\n    Wait For Activation\nELSE\n    Fail    Unexpected status: ${status}\nEND\n# Explicit handling of unexpected cases</code></div></p>\n\n<p class=\"story\">ELSE acts as a <strong>catch-all</strong> for cases not explicitly handled.</p>\n\n<div class=\"section-heading\">Complex Conditions in ELSE IF</div>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nComplex Branching\n    ${age}        Set Variable    25\n    ${country}    Set Variable    US\n    ${verified}   Set Variable    ${True}\n    \n    IF    ${age} >= 21 and '${country}' == 'US' and ${verified}\n        Log    Full access granted\n        Grant All Permissions\n    ELSE IF    ${age} >= 18 and ${verified}\n        Log    Standard access granted\n        Grant Standard Permissions\n    ELSE IF    ${age} >= 13\n        Log    Teen access with restrictions\n        Grant Limited Permissions\n    ELSE\n        Log    Parental consent required\n        Require Guardian Approval\n    END</code></div>\n\n<p class=\"story\">Each condition can be as <strong>simple or complex</strong> as needed.</p>\n\n<div class=\"section-heading\">Common Patterns</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Range Checking (Number Categories)</div></p>\n\n<div class=\"code-block\"><code># Categorize values into ranges\nIF    ${value} > 100\n    Log    High\nELSE IF    ${value} > 50\n    Log    Medium\nELSE IF    ${value} > 0\n    Log    Low\nELSE\n    Log    Zero or negative\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">String Matching (Multiple Options)</div></p>\n\n<div class=\"code-block\"><code># Match against multiple string values\nIF    '${action}' == 'create'\n    Create Resource\nELSE IF    '${action}' == 'update'\n    Update Resource\nELSE IF    '${action}' == 'delete'\n    Delete Resource\nELSE\n    Fail    Invalid action: ${action}\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Fallback Logic</div></p>\n\n<div class=\"code-block\"><code># Try options in order of preference\nIF    API V3 Available\n    Use API V3\nELSE IF    API V2 Available\n    Log    API V3 unavailable, using V2\n    Use API V2\nELSE IF    API V1 Available\n    Log    API V2 unavailable, using V1 (deprecated)\n    Use API V1\nELSE\n    Fail    No API versions available\nEND</code></div>\n\n<div class=\"section-heading\">Best Practices</div>\n\n<p class=\"story\">#<div class=\"section-heading\">1. Order From Most to Least Specific</div></p>\n\n<div class=\"code-block\"><code># Correct: Specific to general\nIF    ${age} == 21           # Exact match first\nELSE IF    ${age} >= 21      # Range second\nELSE IF    ${age} >= 18      # Broader range third\nELSE                          # Catch-all last\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">2. Always Include Final ELSE</div></p>\n\n<div class=\"code-block\"><code># Good: Handles unexpected cases\nIF    '${env}' == 'prod'\n    Use Production Config\nELSE IF    '${env}' == 'staging'\n    Use Staging Config\nELSE\n    Fail    Unknown environment: ${env}\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">3. Keep Condition Logic Clear</div></p>\n\n<div class=\"code-block\"><code># Poor: Hard to read\nIF    ${x} > 90 or (${y} < 10 and ${z})\n    Do Something\nELSE IF    ${x} <= 90 and ${y} >= 10\n    Do Another Thing\nEND\n\n<p class=\"story\"># Better: Extract to variables\n${high_priority}    Evaluate    ${x} > 90 or (${y} < 10 and ${z})\n${medium_priority}  Evaluate    ${x} <= 90 and ${y} >= 10</p>\n\n<p class=\"story\">IF    ${high_priority}\n    Handle High Priority\nELSE IF    ${medium_priority}\n    Handle Medium Priority\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">4. Log Which Branch Executes</div></p>\n\n<div class=\"code-block\"><code>IF    ${score} >= 90\n    Log    Branch: Grade A (score ${score})\nELSE IF    ${score} >= 70\n    Log    Branch: Grade B (score ${score})\nELSE\n    Log    Branch: Grade C or below (score ${score})\nEND</code></div>\n\n<p class=\"story\">By mastering IF-ELSE IF-ELSE, you can implement <strong>sophisticated decision trees</strong> that handle complex business logic, multi-state workflows, and comprehensive error handling in your test automation.</p>",
        "learningObjectives": [
          "Implement multi-branch conditional logic with ELSE IF clauses",
          "Understand first-match-wins execution flow in condition chains",
          "Order conditions correctly from most to least specific",
          "Use final ELSE as a catch-all for unhandled cases"
        ],
        "relatedConcepts": [
          "Complex condition expressions",
          "Condition ordering and precedence",
          "Switch/case alternatives in other languages",
          "Error handling with default branches",
          "Performance of condition chains"
        ],
        "documentation": "https://robotframework.org/robotframework/latest/RobotFrameworkUserGuide.html#if-else-syntax",
        "estimatedReadTime": "7-8 minutes"
      },
      "metadata": {
        "tags": [
          "beginner",
          "control-flow",
          "else-if",
          "multi-branch",
          "conditional-logic",
          "decision-tree",
          "condition-chains",
          "first-match"
        ],
        "estimatedTime": "12-15 minutes",
        "prerequisites": [
          "ex-4-2"
        ],
        "nextExercises": [
          "ex-4-4"
        ],
        "version": "2.0",
        "lastUpdated": "2025-10-02"
      }
    },
    {
      "id": "ex-4-4",
      "title": "FOR Loop - List Iteration",
      "difficulty": "beginner",
      "chapter": "5",
      "free": true,
      "description": "Iterate over a list of items.",
      "instructions": [
        "Create a list ${colors} with: red, green, blue",
        "Use FOR loop to iterate: FOR ${color} IN @{colors}",
        "Log each color",
        "Close with END"
      ],
      "initialCode": "*** Test Cases ***\nLoop Over List\n    ${colors}    Create List    red    green    blue\n    # Write FOR loop here\n    ",
      "solution": "*** Test Cases ***\nLoop Over List\n    ${colors}    Create List    red    green    blue\n    FOR    ${color}    IN    @{colors}\n        Log    ${color}\n    END",
      "hints": [
        "FOR loop syntax: FOR ${var} IN @{list}",
        "Use @{list} to expand list items",
        "Code inside FOR is indented 8 spaces",
        "Don't forget END"
      ],
      "validation": {
        "mustContain": [
          "FOR",
          "IN",
          "@{colors}",
          "END"
        ],
        "mustPass": true
      },
      "theory": {
        "title": "Iteration with FOR Loops in Robot Framework",
        "content": "<div class=\"section-heading\">Introduction to Loops</div>\n\n<p class=\"story\">Loops enable <strong>repeated execution</strong> of code blocks, eliminating repetition and enabling dynamic test scenarios. Robot Framework's FOR loop (modernized in RF 3.1+) provides intuitive iteration over lists, ranges, and other collections.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Why Loops Matter in Testing</div></p>\n\n<p class=\"story\"><strong>Without loops (repetitive):</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTest Colors Manually\n    Log    red\n    Log    green\n    Log    blue\n    # What if we have 100 colors? Unmaintainable!</code></div></p>\n\n<p class=\"story\"><strong>With loops (scalable):</strong>\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTest Colors Dynamically\n    ${colors}    Create List    red    green    blue\n    FOR    ${color}    IN    @{colors}\n        Log    ${color}\n    END\n    # Easily handles 3, 100, or 1000 items!</code></div></p>\n\n<p class=\"story\">Loops make tests <strong>scalable</strong>, <strong>maintainable</strong>, and <strong>data-driven</strong>.</p>\n\n<div class=\"section-heading\">FOR Loop Syntax</div>\n\n<div class=\"code-block\"><code>FOR    ${variable}    IN    @{list}\n    <keywords to repeat>\nEND</code></div>\n\n<p class=\"story\"><strong>Components:</strong>\n- <span class=\"inline-code\">FOR</span> keyword starts the loop\n- <span class=\"inline-code\">${variable}</span> receives each item (one at a time)\n- <span class=\"inline-code\">IN</span> separator keyword\n- <span class=\"inline-code\">@{list}</span> the list variable to iterate (@ expands the list)\n- Loop body indented <strong>8 spaces</strong> (double indent)\n- <span class=\"inline-code\">END</span> closes the loop</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Basic Example</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nColor Iteration\n    ${colors}    Create List    red    green    blue\n    FOR    ${color}    IN    @{colors}\n        Log    Current color: ${color}\n    END</code></div>\n\n<p class=\"story\"><strong>Execution:</strong>\n<div class=\"code-block\"><code>1. ${color} = 'red'   -> Log \"Current color: red\"\n2. ${color} = 'green' -> Log \"Current color: green\"\n3. ${color} = 'blue'  -> Log \"Current color: blue\"\n4. Loop ends</code></div></p>\n\n<div class=\"section-heading\">List Variables: @ vs $</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Creating Lists</div></p>\n\n<div class=\"code-block\"><code># Using Create List keyword\n${colors}    Create List    red    green    blue\n\n<p class=\"story\"># Using @ notation in Variables section\n<strong>* Variables </strong>*\n@{COLORS}    red    green    blue</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Accessing Lists</div></p>\n\n<p class=\"story\"><strong>${list} - Scalar access (entire list):</strong>\n<div class=\"code-block\"><code>${colors}    Create List    red    green    blue\nLog    ${colors}    # Logs: ['red', 'green', 'blue']</code></div></p>\n\n<p class=\"story\"><strong>@{list} - List expansion (individual items):</strong>\n<div class=\"code-block\"><code>${colors}    Create List    red    green    blue\nLog Many    @{colors}    # Logs: red, green, blue (three separate arguments)</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Why @{list} in FOR Loops?</div></p>\n\n<p class=\"story\">The <span class=\"inline-code\">@{list}</span> syntax <strong>expands</strong> the list into individual items:</p>\n\n<div class=\"code-block\"><code># Correct: @{colors} expands to individual items\nFOR    ${color}    IN    @{colors}\n    Log    ${color}    # Logs each color separately\nEND\n\n<p class=\"story\"># Wrong: ${colors} passes the entire list as one item\nFOR    ${color}    IN    ${colors}\n    Log    ${color}    # Logs the entire list ['red', 'green', 'blue'] once\nEND</code></div></p>\n\n<p class=\"story\"><strong>Rule:</strong> Use <span class=\"inline-code\">@{list}</span> to iterate over items, <span class=\"inline-code\">${list}</span> to reference the list as a whole.</p>\n\n<div class=\"section-heading\">Practical Applications</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Testing Multiple Users</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTest Login For All Users\n    @{users}    Create List    alice    bob    charlie    diana\n    \n    FOR    ${user}    IN    @{users}\n        Log    Testing login for: ${user}\n        Login As User    ${user}    password123\n        Verify Dashboard Visible\n        Logout\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Validating Multiple URLs</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nCheck All Pages Are Accessible\n    @{urls}    Create List\n    ...    https://example.com/home\n    ...    https://example.com/about\n    ...    https://example.com/contact\n    ...    https://example.com/products\n    \n    FOR    ${url}    IN    @{urls}\n        Open Browser    ${url}    Chrome\n        Page Should Contain Element    xpath://body\n        Close Browser\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Processing Test Data</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nValidate Product Prices\n    ${products}    Get Products From Database\n    \n    FOR    ${product}    IN    @{products}\n        ${price}    Get From Dictionary    ${product}    price\n        Should Be True    ${price} > 0    msg=Price must be positive\n        Log    ${product}[name]: $${price}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Bulk Data Creation</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nCreate Test Accounts\n    @{emails}    Create List\n    ...    user1@test.com\n    ...    user2@test.com\n    ...    user3@test.com\n    \n    FOR    ${email}    IN    @{emails}\n        Create User Account    ${email}    password=default123\n        Verify Account Created    ${email}\n    END\n    \n    [Teardown]    Delete All Test Accounts</code></div>\n\n<div class=\"section-heading\">Inline Lists (Direct Iteration)</div>\n\n<p class=\"story\">You can iterate over items <strong>without creating a variable</strong>:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nTest Browsers\n    FOR    ${browser}    IN    Chrome    Firefox    Edge\n        Open Browser    https://example.com    ${browser}\n        Page Should Contain    Welcome\n        Close Browser\n    END</code></div>\n\n<p class=\"story\">Here, <span class=\"inline-code\">IN Chrome Firefox Edge</span> provides items directly.</p>\n\n<div class=\"section-heading\">Multiple Variables Per Iteration</div>\n\n<p class=\"story\">Iterate over <strong>grouped items</strong> using multiple variables:</p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nLogin Test Cases\n    FOR    ${username}    ${password}    ${expected}    IN\n    ...    alice       pass123    success\n    ...    bob         wrong      failure\n    ...    charlie     pass456    success\n        Attempt Login    ${username}    ${password}\n        Verify Result    ${expected}\n    END</code></div>\n\n<p class=\"story\"><strong>Pattern:</strong> Items are consumed in <strong>groups of N</strong> (N = number of variables).</p>\n\n<div class=\"section-heading\">Loop Indentation</div>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nIndentation Example\n····${colors}····Create·List····red····green       # 4 spaces (test level)\n····FOR····${color}····IN····@{colors}             # 4 spaces (FOR keyword)\n········Log····${color}                             # 8 spaces (loop body)\n········Should·Not·Be·Empty····${color}            # 8 spaces (loop body)\n····END                                             # 4 spaces (END keyword)</code></div>\n\n<p class=\"story\"><strong>Rules:</strong>\n- FOR keyword: 4 spaces (test case level)\n- Loop body: 8 spaces (inside FOR)\n- END keyword: 4 spaces (closes loop)</p>\n\n<div class=\"section-heading\">Common Patterns</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Enumerate Pattern (Index + Item)</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nLog With Index\n    ${colors}    Create List    red    green    blue\n    ${index}     Set Variable    ${0}\n    \n    FOR    ${color}    IN    @{colors}\n        ${index}    Evaluate    ${index} + 1\n        Log    Item ${index}: ${color}\n    END\n    # Output:\n    # Item 1: red\n    # Item 2: green\n    # Item 3: blue</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Conditional Processing in Loop</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nProcess Selected Items\n    ${items}    Create List    apple    banana    orange    grape\n    \n    FOR    ${item}    IN    @{items}\n        IF    '${item}' == 'banana'\n            Log    Skipping ${item}\n            CONTINUE\n        END\n        Log    Processing ${item}\n        Process Item    ${item}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Accumulation Pattern</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nCalculate Total\n    ${prices}    Create List    ${10}    ${20}    ${30}    ${40}\n    ${total}     Set Variable    ${0}\n    \n    FOR    ${price}    IN    @{prices}\n        ${total}    Evaluate    ${total} + ${price}\n    END\n    \n    Log    Total: ${total}    # Logs: Total: 100\n    Should Be Equal As Numbers    ${total}    ${100}</code></div>\n\n<div class=\"section-heading\">Best Practices</div>\n\n<p class=\"story\">#<div class=\"section-heading\">1. Use Descriptive Loop Variables</div></p>\n\n<div class=\"code-block\"><code># Poor: Generic names\nFOR    ${item}    IN    @{list}\n    Log    ${item}\nEND\n\n<p class=\"story\"># Good: Descriptive names\nFOR    ${username}    IN    @{test_users}\n    Log    Testing user: ${username}\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">2. Keep Loop Bodies Simple</div></p>\n\n<div class=\"code-block\"><code># Poor: Complex logic in loop\nFOR    ${user}    IN    @{users}\n    # 30 lines of complex operations\nEND\n\n<p class=\"story\"># Good: Extract to keyword\nFOR    ${user}    IN    @{users}\n    Test User Account    ${user}\nEND</p>\n\n<p class=\"story\"><strong>* Keywords </strong>*\nTest User Account\n    [Arguments]    ${user}\n    # Complex logic here</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">3. Always Close with END</div></p>\n\n<div class=\"code-block\"><code># Wrong: Missing END\nFOR    ${color}    IN    @{colors}\n    Log    ${color}\n# Next keyword will cause error!\n\n<p class=\"story\"># Correct\nFOR    ${color}    IN    @{colors}\n    Log    ${color}\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">4. Use @{list} for Expansion</div></p>\n\n<div class=\"code-block\"><code># Correct: @{list} expands items\nFOR    ${item}    IN    @{items}\n    Process    ${item}\nEND\n\n<p class=\"story\"># Wrong: ${list} treats whole list as one item\nFOR    ${item}    IN    ${items}\n    Process    ${item}    # Processes entire list at once!\nEND</code></div></p>\n\n<p class=\"story\">By mastering FOR loops with list iteration, you enable <strong>data-driven testing</strong>, <strong>bulk operations</strong>, and <strong>scalable test automation</strong> that handles dynamic datasets efficiently.</p>",
        "learningObjectives": [
          "Iterate over list items using FOR loops",
          "Understand the difference between @{list} expansion and ${list} scalar access",
          "Apply proper 8-space indentation for loop bodies",
          "Use FOR loops for data-driven and bulk test operations"
        ],
        "relatedConcepts": [
          "FOR loops with ranges (IN RANGE)",
          "List manipulation keywords (Append, Get From List, etc.)",
          "BREAK and CONTINUE for loop control",
          "Multiple variables per iteration",
          "Legacy :FOR syntax (RF 2.x)"
        ],
        "documentation": "https://robotframework.org/robotframework/latest/RobotFrameworkUserGuide.html#for-loops",
        "estimatedReadTime": "7-8 minutes"
      },
      "metadata": {
        "tags": [
          "beginner",
          "control-flow",
          "for-loop",
          "iteration",
          "lists",
          "list-expansion",
          "data-driven",
          "bulk-operations"
        ],
        "estimatedTime": "12-15 minutes",
        "prerequisites": [
          "ex-1-4"
        ],
        "nextExercises": [
          "ex-4-5"
        ],
        "version": "2.0",
        "lastUpdated": "2025-10-02"
      }
    },
    {
      "id": "ex-4-5",
      "title": "FOR Loop - Range",
      "difficulty": "beginner",
      "chapter": "5",
      "free": true,
      "description": "Use FOR loop with a numeric range.",
      "instructions": [
        "Create a FOR loop: FOR ${i} IN RANGE 5",
        "Log the value of ${i}",
        "This will log numbers 0-4"
      ],
      "initialCode": "*** Test Cases ***\nLoop With Range\n    # Write FOR loop with RANGE here\n    ",
      "solution": "*** Test Cases ***\nLoop With Range\n    FOR    ${i}    IN RANGE    5\n        Log    ${i}\n    END",
      "hints": [
        "IN RANGE generates numbers from 0 to n-1",
        "IN RANGE 5 gives: 0, 1, 2, 3, 4",
        "You can use IN RANGE start end for custom ranges",
        "Example: IN RANGE 1 10 gives 1-9"
      ],
      "validation": {
        "mustContain": [
          "FOR",
          "IN RANGE",
          "END"
        ],
        "mustPass": true
      },
      "theory": {
        "title": "Numeric Iteration with FOR IN RANGE",
        "content": "<div class=\"section-heading\">Range-Based Loops</div>\n\n<p class=\"story\">While list iteration handles collections of items, many automation scenarios require <strong>numeric iteration</strong>—repeating actions a specific number of times or over a numeric sequence. The <span class=\"inline-code\">IN RANGE</span> variant of FOR loops provides Python-like range functionality for numeric iteration.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Why Numeric Ranges?</div></p>\n\n<p class=\"story\"><strong>Count-based repetition:</strong>\n<div class=\"code-block\"><code># Retry an operation 5 times\nFOR    ${attempt}    IN RANGE    5\n    ${status}    Try API Call\n    IF    ${status} == 200\n        BREAK\n    END\nEND</code></div></p>\n\n<p class=\"story\"><strong>Sequential processing:</strong>\n<div class=\"code-block\"><code># Process pages 1 through 10\nFOR    ${page}    IN RANGE    1    11\n    Load Page    ${page}\n    Extract Data\nEND</code></div></p>\n\n<p class=\"story\"><strong>Index-based operations:</strong>\n<div class=\"code-block\"><code># Access list elements by index\nFOR    ${i}    IN RANGE    ${list_length}\n    ${item}    Get From List    ${items}    ${i}\n    Process Item    ${item}    index=${i}\nEND</code></div></p>\n\n<p class=\"story\">Range-based loops provide <strong>precise numeric control</strong> for repetitive operations.</p>\n\n<div class=\"section-heading\">IN RANGE Syntax</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Single Argument: 0 to N-1</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    5\n    Log    ${i}\nEND\n# Output: 0, 1, 2, 3, 4</code></div>\n\n<p class=\"story\"><strong>Pattern:</strong> <span class=\"inline-code\">IN RANGE N</span> generates numbers from <strong>0</strong> to <strong>N-1</strong> (N iterations total).</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Two Arguments: Start to End-1</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    3    8\n    Log    ${i}\nEND\n# Output: 3, 4, 5, 6, 7</code></div>\n\n<p class=\"story\"><strong>Pattern:</strong> <span class=\"inline-code\">IN RANGE start end</span> generates numbers from <strong>start</strong> to <strong>end-1</strong>.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Three Arguments: Start to End-1 with Step</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    0    10    2\n    Log    ${i}\nEND\n# Output: 0, 2, 4, 6, 8</code></div>\n\n<p class=\"story\"><strong>Pattern:</strong> <span class=\"inline-code\">IN RANGE start end step</span> generates numbers from <strong>start</strong> to <strong>end-1</strong>, incrementing by <strong>step</strong>.</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Summary Table</div></p>\n\n<p class=\"story\">| Syntax | Example | Output | Description |\n|--------|---------|--------|-------------|\n| <span class=\"inline-code\">IN RANGE N</span> | <span class=\"inline-code\">IN RANGE 5</span> | 0, 1, 2, 3, 4 | 0 to N-1 |\n| <span class=\"inline-code\">IN RANGE start end</span> | <span class=\"inline-code\">IN RANGE 3 8</span> | 3, 4, 5, 6, 7 | start to end-1 |\n| <span class=\"inline-code\">IN RANGE start end step</span> | <span class=\"inline-code\">IN RANGE 0 10 2</span> | 0, 2, 4, 6, 8 | start to end-1 by step |</p>\n\n<div class=\"section-heading\">How RANGE Works (Python Equivalent)</div>\n\n<p class=\"story\">Robot Framework's <span class=\"inline-code\">IN RANGE</span> mirrors Python's <span class=\"inline-code\">range()</span> function:</p>\n\n<p class=\"story\">``<span class=\"inline-code\">python\n# Python equivalent\nfor i in range(5):        # 0, 1, 2, 3, 4\n    print(i)</p>\n\n<p class=\"story\">for i in range(3, 8):     # 3, 4, 5, 6, 7\n    print(i)</p>\n\n<p class=\"story\">for i in range(0, 10, 2): # 0, 2, 4, 6, 8\n    print(i)\n</span>`<span class=\"inline-code\"></p>\n\n<p class=\"story\"><strong>Key behaviors:</strong>\n- <strong>Exclusive end:</strong> Upper bound is <strong>not included</strong> (range(5) → 0-4, not 0-5)\n- <strong>Default start:</strong> If only one argument, start is 0\n- <strong>Default step:</strong> If no step specified, step is 1</p>\n\n<div class=\"section-heading\">Practical Applications</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Retry Logic</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nRetry API Call\n    FOR    ${attempt}    IN RANGE    3\n        Log    Attempt ${attempt + 1} of 3\n        ${status}    ${response}    Run Keyword And Ignore Error    Make API Call\n        \n        IF    '${status}' == 'PASS'\n            Log    API call succeeded on attempt ${attempt + 1}\n            BREAK\n        END\n        \n        IF    ${attempt} < 2    # Not the last attempt\n            Log    Retrying in 2 seconds...\n            Sleep    2s\n        END\n    END\n    \n    Should Be Equal    ${status}    PASS    msg=API call failed after 3 attempts</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Pagination</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nProcess All Pages\n    ${total_pages}    Get Total Page Count\n    \n    FOR    ${page}    IN RANGE    1    ${total_pages + 1}\n        Log    Processing page ${page} of ${total_pages}\n        Navigate To Page    ${page}\n        ${items}    Extract Page Items\n        Process Items    ${items}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Batch Processing</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nProcess In Batches\n    ${total_items}    Set Variable    ${100}\n    ${batch_size}     Set Variable    ${10}\n    \n    FOR    ${start}    IN RANGE    0    ${total_items}    ${batch_size}\n        ${end}    Evaluate    min(${start} + ${batch_size}, ${total_items})\n        Log    Processing items ${start} to ${end - 1}\n        ${batch}    Get Items    ${start}    ${end}\n        Process Batch    ${batch}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Countdown Timer</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nCountdown\n    FOR    ${seconds}    IN RANGE    5    0    -1\n        Log    ${seconds} seconds remaining...\n        Sleep    1s\n    END\n    Log    Time's up!</code></div>\n\n<p class=\"story\"><strong>Note:</strong> Negative step for reverse iteration (5, 4, 3, 2, 1).</p>\n\n<p class=\"story\">#<div class=\"section-heading\">Creating Sequential Data</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nCreate Test Users\n    FOR    ${i}    IN RANGE    1    11\n        ${username}    Set Variable    testuser${i}\n        ${email}       Set Variable    testuser${i}@example.com\n        Create User    ${username}    ${email}\n        Log    Created user: ${username}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Index-Based List Access</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nProcess List By Index\n    ${items}    Create List    apple    banana    cherry    date    elderberry\n    ${length}   Get Length    ${items}\n    \n    FOR    ${i}    IN RANGE    ${length}\n        ${item}    Get From List    ${items}    ${i}\n        Log    Index ${i}: ${item}\n        \n        IF    ${i} % 2 == 0\n            Log    ${item} is at even index\n        END\n    END</code></div>\n\n<div class=\"section-heading\">Advanced Patterns</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Nested Ranges (Matrix Iteration)</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nGenerate Coordinate Grid\n    FOR    ${x}    IN RANGE    3\n        FOR    ${y}    IN RANGE    3\n            Log    Coordinate: (${x}, ${y})\n        END\n    END\n    # Output:\n    # (0, 0), (0, 1), (0, 2)\n    # (1, 0), (1, 1), (1, 2)\n    # (2, 0), (2, 1), (2, 2)</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Dynamic Range Limits</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nDynamic Iteration\n    ${max_attempts}    Get From Config    max_retries    default=5\n    \n    FOR    ${i}    IN RANGE    ${max_attempts}\n        Attempt Operation    ${i}\n    END</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Combining Range with Conditions</div></p>\n\n<div class=\"code-block\"><code><strong>* Test Cases </strong>*\nFind First Match\n    FOR    ${i}    IN RANGE    100\n        ${value}    Calculate    ${i}\n        \n        IF    ${value} > 500\n            Log    Found match at index ${i}: ${value}\n            BREAK\n        END\n    END</code></div>\n\n<div class=\"section-heading\">Common Range Patterns</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Repeat N Times</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    10\n    Click Button    refresh    # Repeat 10 times\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">1 to N (Not 0 to N-1)</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    1    11    # Start at 1, end at 10\n    Log    Iteration ${i}\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Even Numbers</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    0    20    2\n    Log    ${i}    # 0, 2, 4, 6, ..., 18\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Odd Numbers</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    1    20    2\n    Log    ${i}    # 1, 3, 5, 7, ..., 19\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">Reverse Order</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    10    0    -1\n    Log    ${i}    # 10, 9, 8, ..., 1\nEND</code></div>\n\n<div class=\"section-heading\">Common Mistakes</div>\n\n<p class=\"story\">#<div class=\"section-heading\">Off-by-One Errors</div></p>\n\n<div class=\"code-block\"><code># Wrong: Loops 0-4 (5 iterations) instead of 1-5\nFOR    ${i}    IN RANGE    5\n    Log    Page ${i}    # Logs: Page 0, Page 1, ..., Page 4\nEND\n\n<p class=\"story\"># Correct: Loops 1-5\nFOR    ${i}    IN RANGE    1    6\n    Log    Page ${i}    # Logs: Page 1, Page 2, ..., Page 5\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Inclusive vs. Exclusive Upper Bound</div></p>\n\n<div class=\"code-block\"><code># Wrong: Expects 10 but gets 9\nFOR    ${i}    IN RANGE    1    10\n    Log    ${i}    # Logs 1-9 (not 10!)\nEND\n\n<p class=\"story\"># Correct: Add 1 to upper bound\nFOR    ${i}    IN RANGE    1    11\n    Log    ${i}    # Logs 1-10\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">Forgetting Step Sign</div></p>\n\n<div class=\"code-block\"><code># Wrong: Positive step with reverse range (no iterations)\nFOR    ${i}    IN RANGE    10    0    1\n    Log    ${i}    # Never executes!\nEND\n\n<p class=\"story\"># Correct: Negative step for reverse\nFOR    ${i}    IN RANGE    10    0    -1\n    Log    ${i}    # Works: 10, 9, 8, ..., 1\nEND</code></div></p>\n\n<div class=\"section-heading\">Best Practices</div>\n\n<p class=\"story\">#<div class=\"section-heading\">1. Use Descriptive Variable Names</div></p>\n\n<div class=\"code-block\"><code># Poor\nFOR    ${i}    IN RANGE    5\n    Log    ${i}\nEND\n\n<p class=\"story\"># Good\nFOR    ${attempt}    IN RANGE    5\n    Log    Attempt ${attempt + 1}\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">2. Make Range Limits Clear</div></p>\n\n<div class=\"code-block\"><code># Unclear magic number\nFOR    ${i}    IN RANGE    100\n    ...\nEND\n\n<p class=\"story\"># Clear with variable\n${MAX_RETRIES}    Set Variable    ${100}\nFOR    ${i}    IN RANGE    ${MAX_RETRIES}\n    ...\nEND</code></div></p>\n\n<p class=\"story\">#<div class=\"section-heading\">3. Document Unusual Ranges</div></p>\n\n<div class=\"code-block\"><code># Complex step pattern\nFOR    ${i}    IN RANGE    10    100    15\n    # Process every 15th item from 10 to 99\n    Process Item    ${i}\nEND</code></div>\n\n<p class=\"story\">#<div class=\"section-heading\">4. Use 1-Based Indexing for Human-Readable Logs</div></p>\n\n<div class=\"code-block\"><code>FOR    ${i}    IN RANGE    ${item_count}\n    Log    Processing item ${i + 1} of ${item_count}    # Human-friendly: 1, 2, 3...\n    ${item}    Get From List    ${items}    ${i}         # Zero-indexed access\nEND</code></div>\n\n<p class=\"story\">By mastering </span>IN RANGE`, you gain precise control over <strong>numeric iteration</strong>, enabling retry logic, pagination, batch processing, and count-based test automation patterns.</p>",
        "learningObjectives": [
          "Use IN RANGE for numeric iteration in FOR loops",
          "Understand range syntax variations (N, start-end, start-end-step)",
          "Apply range-based loops for retry logic, pagination, and batch processing",
          "Avoid off-by-one errors with exclusive upper bounds"
        ],
        "relatedConcepts": [
          "Python range() function equivalence",
          "Index-based list access",
          "Retry patterns and timeout loops",
          "Pagination and batch processing",
          "Negative step for reverse iteration"
        ],
        "documentation": "https://robotframework.org/robotframework/latest/RobotFrameworkUserGuide.html#for-loops",
        "estimatedReadTime": "7-8 minutes"
      },
      "metadata": {
        "tags": [
          "beginner",
          "control-flow",
          "for-loop",
          "in-range",
          "numeric-iteration",
          "retry-logic",
          "pagination",
          "batch-processing"
        ],
        "estimatedTime": "12-15 minutes",
        "prerequisites": [
          "ex-4-4"
        ],
        "nextExercises": [
          "ex-4-6"
        ],
        "version": "2.0",
        "lastUpdated": "2025-10-02"
      }
    }
  ]
}